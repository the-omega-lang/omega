# Both templates match at zero cost and have empty bound sets.
# Parameter structure does not break the tie.
# See functions.md, Overloading.
free<T>(ptr: *T) => void { }
free<T>(ptr: T) => void { }

main() => void {
    value := 1u8;
    free(&value);
}
