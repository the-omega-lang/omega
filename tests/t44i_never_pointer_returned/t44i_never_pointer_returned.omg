import std::io::println;

@naked
returns_anyway() => never {
    asm() => {
        ret
    }
}

main() => void {
    indirect: () => never = returns_anyway;
    pointer: *() => never = &indirect;
    println$("before-call");
    (*pointer)();
    println$("after-call");
}
