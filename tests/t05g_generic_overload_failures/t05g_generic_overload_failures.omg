spec A { marker(*self) => void; }
marker Value { }
selected<T: A>(value: T) => void { }
selected<T>(value: T) => void { }
needs<T = u64>(a: T, b: T) => void { }
needs(value: bool) => void { }
main() => void {
    selected(Value { });
    needs(1, 2u32);
}
