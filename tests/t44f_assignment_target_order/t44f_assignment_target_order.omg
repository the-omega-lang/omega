import std::io::println;
select_target(value: *mut i32) => *mut i32 {
    println$("target-before");
    value
}
main() => void {
    mut value: i32 = 0;
    *select_target(&mut value) = if true { panic$("rhs"); } else { 1 };
    println$("after-store: ", value);
}
