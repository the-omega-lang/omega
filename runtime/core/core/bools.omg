# `core::bools` currently provides only Display; classification helpers are a
# separate concern.

import fmt::Display;
import fmt::write_bool;
import io::Writer;

compose bool : Display {
    fmt(*self, out: *mut Writer) => void {
        write_bool(out, *self);
    }
}
