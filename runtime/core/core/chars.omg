# `core::chars` currently provides only UTF-8 Display formatting.

import fmt::Display;
import fmt::write_char;
import io::Writer;

exposed spec CharOps : Display for char {
    fmt(*self, out: *mut Writer) => void {
        write_char(out, *self);
    }
}
