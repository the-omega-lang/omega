# `core::chars` currently provides only UTF-8 Display formatting.

import fmt::Display;
import fmt::write_char;
import io::Writer;

primitive char {
    exposed fmt(*self, out: *mut Writer) => void {
        write_char(out, *self);
    }
}

compose char : Display {}
