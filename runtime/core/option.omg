# `core::option` -- a generic "maybe a value" type. Variant order is
# load-bearing: `for..in` desugaring hardcodes `None`=0, `Some`=1
# (see `Analyzer::analyze_for_in`); don't reorder.

exposed enum Option<T> {
    None,
    Some { exposed value: T; };
}
