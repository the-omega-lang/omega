# `std::default` -- the canonical "zero value" for a type, reached through a
# name (`T::default()`) rather than a per-type literal. Same role as Rust's
# `Default` trait.

exposed spec Default {
    default() => Self;
}
